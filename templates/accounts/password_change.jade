extends "base.html"

block content
    .form-view
        h1 Password change
        p Please enter your old password, for security's sake, and then enter your 
            | new password twice so we can verify you typed it in correctly.
        form.simple-form(action="",method="post")
            {% csrf_token %}
            {{ form.as_p }}
            input.btn.btn-sm(type="submit",value="update")
